<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/Theme.ObsidianWidget"
        android:allowBackup="true">
        <activity android:name=".setup.SetupActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- Own task: opening settings from a widget shouldn't resurface the app
             task behind it (back/done should return to the home screen). -->
        <activity
            android:name=".config.WidgetConfigActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity="">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
        <!-- Empty taskAffinity: launching with NEW_TASK must NOT bring the app's
             existing task to the foreground — the trampoline runs in its own
             invisible throwaway task. -->
        <activity
            android:name=".widget.WidgetActionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:taskAffinity=""
            android:theme="@android:style/Theme.NoDisplay" />
        <activity
            android:name=".editor.EditorActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.ObsidianWidget.Dialog" />
        <receiver android:name=".widget.NoteWidgetProvider" android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/note_widget_info" />
        </receiver>
        <service
            android:name=".widget.NoteWidgetService"
            android:permission="android.permission.BIND_REMOTEVIEWS" />
    </application>
</manifest>
